
window.onload = function () {
    setTimeout(function () {
        var container = document.querySelector('.container');
        if (container) {
            container.style.display = 'block';
        }
    }, 10); // Optional delay (100ms) for better rendering
};
